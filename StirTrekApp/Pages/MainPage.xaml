<phone:PhoneApplicationPage 
    x:Class="StirTrekApp.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="768"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    shell:SystemTray.IsVisible="True"
    x:Name="myPage" Background="#FF7AA9B3">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot">
        <controls:Pivot>
            <controls:Pivot.Title>
                <TextBlock Text="Stir Trek WP" Foreground="#7AA9B3"></TextBlock>
            </controls:Pivot.Title>
            <controls:PivotItem>
                <controls:PivotItem.Header>
                    <TextBlock Text="Schedule" Foreground="#7AA9B3"></TextBlock>
                </controls:PivotItem.Header>
                <ListBox Name="ScheduleList" Margin="3">
                    <ListBox.ItemTemplate>
                        <DataTemplate x:Name="ScheduleDataTemplate">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition />
                                </Grid.RowDefinitions>

                                <Grid Grid.Row="0" Background="#D5CCC1" Width="{Binding ElementName=myPage, Path=Parent.ActualWidth}">
                                    <TextBlock Style="{StaticResource ScheduleHeader}" Text="{Binding TimeSlot.DisplayTimeRange}"></TextBlock>
                                </Grid>
                                <ListBox SelectionChanged="ScheduleList_OnSelectionChanged" Grid.Row="1" x:Name="ListBox1" ItemsSource="{Binding Sessions}" 
                                         ScrollViewer.VerticalScrollBarVisibility="Disabled" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Margin="0,10,0,10" >
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Vertical" Margin="0,5,0,5">
                                                <TextBlock TextWrapping="Wrap" Text="{Binding Name}" Style="{StaticResource ScheduleSessionEntry}" />
                                                <TextBlock TextWrapping="Wrap" Text="{Binding DisplaySpeakers}" Style="{StaticResource ScheduleAuthorEntry}"/>
                                                <TextBlock Text="{Binding Id}" Visibility="Collapsed"></TextBlock>
                                                <TextBlock TextWrapping="Wrap" Text="{Binding DisplayTrackInfo}" Style="{StaticResource ScheduleLocationEntry}"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </controls:PivotItem>
            <controls:PivotItem Header="Sessions">
               <ListBox Name="SessionList" Margin="3">
                   <ListBox.ItemTemplate>
                       <DataTemplate x:Name="SessionDataTemplate">
                           <Grid>
                               <Grid.RowDefinitions>
                                   <RowDefinition></RowDefinition>
                                   <RowDefinition></RowDefinition>
                               </Grid.RowDefinitions>
                               
                               <TextBlock Grid.Row="0" Text="{Binding Name}" />
                               <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5,0,5">
                                    <TextBlock TextWrapping="Wrap" Text="{Binding DisplaySpeakers}" />
                                    <TextBlock TextWrapping="Wrap" Margin="0,3,0,3" Text="{Binding DisplayTrackInfo}" />
                               </StackPanel>
                               
                           </Grid>
                       </DataTemplate>
                   </ListBox.ItemTemplate>
               </ListBox>
            </controls:PivotItem>
            <controls:PivotItem Header="Speakers">
                <ListBox Name="SpeakerList" Margin="3">
                    <ListBox.ItemTemplate>
                        <DataTemplate x:Name="SpeakerDataTemplate">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition></ColumnDefinition>
                                    <ColumnDefinition></ColumnDefinition>
                                </Grid.ColumnDefinitions>

                                <Image Grid.Column="0" Height="50" Width="50" Source="Content\Speaker\ApplicationIcon.png"></Image>
                                <TextBlock Grid.Column="1" Text="{Binding Name}" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </controls:PivotItem>
            <controls:PivotItem Header="Sponsors">
                <ListBox Name="SponsorList" Margin="3">
                    <ListBox.ItemTemplate>
                        <DataTemplate x:Name="SponsorDataTemplate">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition></ColumnDefinition>
                                    <ColumnDefinition></ColumnDefinition>
                                </Grid.ColumnDefinitions>

                                <Image Grid.Column="0" Height="50" Width="50" Source="Content\Sponsor\ApplicationIcon.png"></Image>
                                <TextBlock Grid.Column="1" Text="{Binding Name}" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </controls:PivotItem>
        </controls:Pivot>
    </Grid>
 
</phone:PhoneApplicationPage>